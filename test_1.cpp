#include<bits/stdc++.h>

using namespace std;

int main() {
  int a = (1 << 5)-1;

  bitset<8> bit(a);
  cout<<bit<<endl;
  int b = (1<<4);
  //if(a == b ) cout<<"true";
  //else cout<<"flase";
  return 0;
}
